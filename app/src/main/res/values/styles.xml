<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toolbar styles -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- /Toolbar styles -->


    <style name="FooterIcon">
        <item name="android:layout_width">@dimen/card_icon_size</item>
        <item name="android:layout_height">@dimen/card_icon_size</item>
    </style>

    <style name="NameStyle">
        <item name="android:textSize">@dimen/text_primary_size</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="DateStyle">
        <item name="android:textSize">@dimen/text_secondary_size</item>
        <item name="android:textColor">@color/colorTextSecondary</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>
